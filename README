        taobao open platform API @Python 
The first step love everything
can you win? wenwen

Usage:
    1. install the package the python path

    2. in file

    :
    from top import TopClient, UserGetRequest

    client = Topclient(key='123456', secret='68kjkflda868699')
    request = UserGetRequest()
    request.set_nick('mydewdew')
    reqeust.set_fields('user_id,nick,sex,locaiton')
    data = client.execute(request)
    
